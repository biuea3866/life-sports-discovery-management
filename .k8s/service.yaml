apiVersion: v1
kind: Service
metadata:
  labels:
    app: discovery-management
  name: discovery-management
  namespace: default
spec:
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000
    selector:
      app: discovery-management
    type: ClusterIP